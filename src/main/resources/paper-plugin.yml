name: Hardcourse
version: '1.0'
main: com.denied403.Hardcourse.Hardcourse
api-version: '1.21.11'
author: 403Denied
contributors:
  - Evelyn344
  - Rockyy321
dependencies:
  server:
    Core403:
      load: BEFORE
      required: true
      join-classpath: true
    PlaceholderAPI:
      load: BEFORE
      required: true
    LuckPerms:
      load: BEFORE
      required: true
description: "Hardcourse plugin for managing player checkpoints and commands."
prefix: "HARDCOURSE"
